services:
  frontend:
    build: .
    ports:
      - "8081:8081"
    environment:
      - EXPO_NO_TELEMETRY=1
      - CI=1
    stdin_open: true
    tty: true
    volumes:
      - .:/app
      - /app/node_modules
