"""Recommendation service package.

pipeline.py and tools.py will be moved here from the backend root once
the migration is complete.  For now they remain at the top level and are
imported directly (e.g. ``from pipeline import recommend``).
"""
